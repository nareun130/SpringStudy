# Spring Data Source 설정 - MYSQL
spring:
    datasource:
        url: "jdbc:mysql://127.0.0.1:3306/example?&useSSL=false&allowPublicKeyRetrieval=true&useUnicode=true&serverTimezone=UTC"
        username: nareun130
        password: u1234
        driver-class-name: com.mysql.cj.jdbc.Driver
    devtools:
        livereload:
            enabled: true
        restart:
            enabled: true
    jpa:
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                show_sql: true
                format_sql: true
        database-platform: org.hibernate.dialect.MySQL8Dialect
